#include <vector>
#include "parser.hpp"

struct Construct {
	expression rayon;
	std::vector<instruction> traitBuild;
};

class prog{
public:
	prog()=default;
	void traite(std::vector<instruction> abrInstruct)
private:

};
